# Java 开发规范（Codex 精简版）

> 来源：阿里巴巴 Java 开发手册 v1.2.0  
> 用途：供 Codex 编码、Review、重构时作为默认规范。  
> 原手册中的示例、背景说明、版本历史、术语解释等已删除，仅保留对实际开发最有价值的规则。

## 1. 命名

- 类名：`UpperCamelCase`，如 `UserService`、`UserDTO`。
- 方法、参数、成员变量、局部变量：`lowerCamelCase`。
- 常量：全大写 + 下划线，如 `MAX_RETRY_COUNT`。
- 抽象类以 `Abstract` 或 `Base` 开头。
- 异常类以 `Exception` 结尾。
- 测试类以被测试类名 + `Test` 结尾。
- 包名全部小写。
- 禁止中文、拼音与英文混合命名。
- 避免无意义、难懂的缩写。
- Service/DAO 接口的实现类使用 `Impl` 后缀。
- Service/DAO 方法命名：
    - 查询单个：`getXxx`
    - 查询多个：`listXxx`
    - 统计：`countXxx`
    - 新增：`saveXxx` / `insertXxx`
    - 删除：`removeXxx` / `deleteXxx`
    - 修改：`updateXxx`
- POJO 类型按职责区分：`DO`、`DTO`、`BO`、`VO`。

## 2. 常量

- 禁止直接使用无意义的魔法值，重要常量应定义为常量或枚举。
- 常量按业务/功能分类，不要维护一个巨大常量类。
- `long/Long` 字面量使用大写 `L`。

## 3. Java 基础/OOP

- 重写方法必须加 `@Override`。
- 禁止使用过时 API。
- 对象比较优先使用 `Objects.equals(a, b)`；确定非空时可使用常量调用 `equals`。
- 包装类型之间比较值使用 `equals`，不要使用 `==`。
- POJO 属性优先使用包装类型；局部变量可使用基本类型。
- DO/DTO/VO 不要设置不必要的属性默认值。
- 构造方法不要放业务逻辑。
- 成员变量、方法尽量使用最小访问权限：能 `private` 就不要 `public`。
- 工具类构造方法应私有。
- getter/setter 不放业务逻辑。
- 避免 `Object`、无意义的可变参数。
- 循环中大量字符串拼接使用 `StringBuilder`。
- 避免重复代码，遵循 DRY。

## 4. 集合

- 重写 `equals` 必须同时重写 `hashCode`。
- 自定义对象作为 `Map key` 时必须正确实现 `equals/hashCode`。
- 不要把 `subList()` 强转成 `ArrayList`。
- 不要在 foreach 中直接 `add/remove` 原集合；删除使用 `Iterator`。
- 使用泛型，理解 `? extends` / `? super`，遵循 PECS。
- Map 遍历优先 `entrySet()`。
- 注意集合是否允许 `null`，`ConcurrentHashMap` 不允许 key/value 为 null。
- 集合初始化时，已知容量应指定初始大小。
- 去重优先使用 `Set`，避免 `List.contains()` 反复遍历。

## 5. 控制流程

- `if/else/for/while/do` 必须使用 `{}`。
- `switch` 必须有 `default`；每个 case 明确 `break/return` 或说明继续执行。
- 避免超过 3 层 `if/else` 嵌套，优先使用卫语句提前返回。
- 复杂条件先计算成有意义的 boolean 变量。
- 循环中避免重复创建对象、获取连接、执行不必要的 try-catch。
- 对外接口、敏感操作、耗时操作必须进行参数校验。

## 6. 代码格式

- 缩进 4 个空格，不使用 Tab。
- 运算符两边留空格。
- 方法参数逗号后留空格。
- 单行代码尽量不超过 120 字符。
- UTF-8 编码，Unix 换行符。
- 不保留无用的注释代码；无用代码直接删除。
- 注释应解释业务含义和核心设计，不要写显而易见的注释。

## 7. 并发与线程

- 线程资源统一使用线程池，不要随意 `new Thread()`。
- 不使用 `Executors` 创建线程池，优先显式使用 `ThreadPoolExecutor`，明确核心线程数、最大线程数、队列、拒绝策略等。
- 线程池/线程名称应具有业务含义。
- `SimpleDateFormat` 非线程安全，不要作为共享静态实例使用。
- JDK8 优先使用 `java.time` API，如 `LocalDateTime`、`DateTimeFormatter`。
- 缩小锁粒度，避免锁住整个方法。
- 持有锁期间不要调用 RPC、远程接口等耗时操作。
- 多资源加锁时保持固定顺序，避免死锁。
- 并发修改同一数据时考虑乐观锁、悲观锁、分布式锁等方案。
- `volatile` 只能保证可见性，不能保证复合操作的原子性。
- `count++` 等并发计数使用 `AtomicInteger` / `AtomicLong` / JDK8 `LongAdder`。
- `ThreadLocal` 使用后注意清理，避免线程池线程复用导致数据污染。

## 8. 异常处理

- 不要用异常代替正常流程控制。
- 能提前校验避免的 `NullPointerException`、`IndexOutOfBoundsException` 等，不要靠 catch 处理。
- 不要用一个大 try-catch 包住大段业务代码。
- catch 后必须处理异常；不处理就继续向上抛。
- 不要直接抛 `new RuntimeException()`、`Exception`、`Throwable`；优先使用有业务含义的自定义异常。
- finally 不允许使用 `return`。
- 资源关闭优先使用 try-with-resources。
- 事务中捕获异常后，如果需要回滚，必须确保事务正确回滚。
- 远程调用、数据库查询等返回结果要考虑 null。
- 避免连续 `obj.getA().getB().getC()` 导致 NPE。
- 内部业务优先使用异常表达失败；对外 HTTP/API 按项目统一错误码规范返回。

## 9. 日志

- 使用 SLF4J，不直接依赖 Log4j/Logback API。
- 日志不要使用字符串 `+` 拼接，使用占位符：
  `logger.info("userId: {}", userId);`
- 异常日志必须包含上下文信息和异常对象：
  `logger.error("处理用户失败，userId={}", userId, e);`
- 不要重复打印同一个异常。
- 生产环境谨慎输出 `info/warn`，禁止大量无意义日志。
- `debug` 日志避免输出敏感信息和大量对象。
- 日志应能帮助定位问题，不要为了“留痕”大量打印。

## 10. 数据库 / SQL

### 通用

- 查询不要无脑 `SELECT *`，明确指定需要的字段。
- 修改/删除数据前先确认查询结果，避免误操作。
- 分页查询如果总数为 0，直接返回，不执行后续查询。
- 避免大批量 `IN`；确需使用时控制集合大小，并根据数据库实际情况评估。
- 避免不必要的大事务，事务范围尽量小。
- 更新时只更新真正发生变化的字段。
- 注意字段类型一致，避免隐式类型转换导致索引失效。
- SQL 参数必须使用参数绑定，禁止拼接用户输入，防止 SQL 注入。
- 数据库查询条件、排序字段等来自前端时必须进行白名单校验。

### 索引

- 业务唯一字段必须建立唯一索引。
- JOIN 字段需要类型一致并合理建立索引。
- 不要为了一个查询就随意创建一个索引，要结合执行计划分析。
- 组合索引需要结合实际查询条件设计。
- 等值条件通常优先于范围条件设计索引顺序。
- 经常用于排序的字段可结合 WHERE 条件设计组合索引。
- 大分页避免无脑 `LIMIT offset, size`，必要时使用延迟关联/游标式分页。
- 查询性能问题先使用 `EXPLAIN` 分析，再决定是否加索引。
- 关注 `type`、`key`、`rows`、`Extra` 等执行计划信息。

### ORM / MyBatis

- 使用 `#{}` 参数绑定，不使用 `${}` 接收用户输入。
- DO/DTO/VO 与数据库字段需要明确映射。
- 查询结果不要直接使用无类型的 `HashMap`/`Hashtable` 作为业务结果。
- 更新记录时同步维护更新时间字段。
- Mapper SQL 尽量只查询实际需要的字段。

> 注意：原手册中的数据库章节主要针对 MySQL。实际项目使用 DM、PostgreSQL 等数据库时，以项目数据库特性和现有规范为准，不要机械套用 MySQL 专属语法或字段类型。

## 11. 分层架构

推荐保持职责清晰：

- Controller/Web：参数接收、基础校验、权限控制、调用 Service。
- Service：核心业务逻辑、事务边界。
- Manager：第三方服务封装、缓存/中间件通用处理、多个 DAO 的组合。
- DAO/Mapper：数据库访问。
- DO：数据库表对应对象。
- DTO：业务层之间的数据传输对象。
- BO：业务对象。
- VO：接口/页面展示对象。

原则：

- 上层依赖下层，避免反向依赖。
- Controller 不写复杂业务。
- DAO 不写业务逻辑。
- Service 不直接堆积大量基础设施代码。
- 第三方调用统一封装，避免业务代码到处直接调用。
- 不要为了分层而过度创建无意义的类。

## 12. 安全

- 所有外部输入必须校验。
- 所有用户权限相关接口必须做权限校验，防止越权。
- 敏感数据展示时必须脱敏。
- SQL 必须参数化，禁止字符串拼接。
- HTML 输出用户输入时必须正确转义/过滤。
- 对登录、验证码、短信、邮件、支付等资源操作增加频率限制和防重放。
- 文件上传、反序列化、正则、重定向、排序字段等输入都要考虑安全风险。
- 不在日志中打印密码、Token、密钥等敏感信息。

## 13. Codex 执行原则

在修改现有项目代码时：

1. **优先遵循项目现有代码风格**，不要为了套规范大规模重构。
2. 修改前先理解现有模块、调用链和数据结构。
3. 只修改完成任务所必需的代码，避免无关改动。
4. 不随意修改已有 API、数据库字段、接口签名和公共方法。
5. 修改后检查编译、测试和潜在异常。
6. 新增代码必须遵循本文规范。
7. 如果项目现有实现与本文规范冲突，优先保证项目现有架构和功能稳定，再局部优化。
8. 不确定时不要猜测业务规则，应从项目代码、配置、数据库映射、测试中确认。